<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Ivan Ads Manager</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">
    <nav class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
            <h1 class="text-xl font-bold">Ivan Ads Manager</h1>
            <a href="/logout" class="text-gray-600 hover:text-gray-800">Logout</a>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 py-8">
        <h2 class="text-2xl font-bold mb-6">Ad Accounts</h2>

        <% if (accounts && accounts.length > 0) { %>
            <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
                <% accounts.forEach(account => { %>
                    <a href="/account/<%= account.id.replace('act_', '') %>"
                       class="block bg-white rounded-lg shadow-sm border hover:shadow-md transition p-6">
                        <h3 class="font-semibold text-lg mb-2"><%= account.name || account.id %></h3>
                        <div class="text-sm text-gray-600 space-y-1">
                            <p>ID: <%= account.id %></p>
                            <% if (account.currency) { %>
                                <p>Currency: <%= account.currency %></p>
                            <% } %>
                            <% if (account.timezone_name) { %>
                                <p>Timezone: <%= account.timezone_name %></p>
                            <% } %>
                            <% if (account.account_status) { %>
                                <p>
                                    Status:
                                    <span class="<%= account.account_status === 1 ? 'text-green-600' : 'text-red-600' %>">
                                        <%= account.account_status === 1 ? 'Active' : 'Inactive' %>
                                    </span>
                                </p>
                            <% } %>
                        </div>
                    </a>
                <% }) %>
            </div>
        <% } else { %>
            <div class="bg-white rounded-lg shadow-sm border p-8 text-center">
                <p class="text-gray-600">No ad accounts found.</p>
                <p class="text-sm text-gray-500 mt-2">
                    Make sure your Meta access token has the required permissions.
                </p>
            </div>
        <% } %>
    </main>
</body>
</html>
